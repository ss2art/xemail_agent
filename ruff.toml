target-version = "py310"
line-length = 120
extend-exclude = ["data", "vectorstore", ".venv"]

[lint]
select = ["E9", "F"]  # Runtime errors and pyflakes (undefined names, etc.)
ignore = ["F401", "F403"]  # Allow intentional re-exports/unused imports in entrypoints
