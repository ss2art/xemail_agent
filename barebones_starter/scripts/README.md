# barebones_starter/scripts

Helper for rebuilding the shared root virtual environment:

- `rebuild_env.sh` — Removes the root `.venv`, recreates it, upgrades packaging tools, and installs `requirements.txt`. Flags: `--help`.
